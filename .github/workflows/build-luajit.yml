name: Build mod-eluna with LuaJIT 🌙

on:
  push:
    branches:
      - 'master'
      - 'action'
  pull_request:

jobs:
  build_lua51:
    uses: ./.github/workflows/core-build-base.yml
    with:
      lua_version: 'luajit'
